# 手机编译详细步骤（无需电脑）

本文档手把手教你用**纯手机操作**完成项目上传和APK获取。

---

## 准备工作

### 你需要
1. 一部安卓手机（安卓7.0以上）
2. 一个 **GitHub 账号**（你已拥有）
3. 下载好的 `mountain-climb-game-v1.tar.gz` 文件
4. 稳定的网络连接（首次需要下载约500MB依赖）

### 必须安装的APP
| APP | 用途 | 下载方式 |
|------|------|----------|
| ZArchiver | 解压 .tar.gz 文件 | 应用商店搜索 "ZArchiver" |
| GitHub 官方APP | 管理仓库 | 应用商店搜索 "GitHub" |
| 浏览器（Chrome/Edge/自带） | 访问GitHub网页 | 已预装 |

---

## 第一步：解压项目文件

### 操作
1. 打开 **ZArchiver**
2. 找到下载目录（通常是 `/Download` 或 `/Downloads`）
3. 点击 `mountain-climb-game-v1.tar.gz`
4. 选择 **解压到此处**（Extract Here）
5. 等待解压完成，你会得到一个 `mountain-climb-game/` 文件夹

### 检查解压结果
进入 `mountain-climb-game/` 文件夹，确认包含：
- `build.gradle`
- `settings.gradle`
- `core/` 文件夹
- `android/` 文件夹
- `.github/workflows/build-apk.yml`

---

## 第二步：创建 GitHub 仓库并上传代码

### 2.1 打开 GitHub APP 创建仓库

1. 打开 **GitHub APP**
2. 点击底部右下角的 **+ 号按钮**（创建）
3. 选择 **新建仓库**（New Repository）
4. 填写仓库信息：
   - **仓库名称**：输入 `mountain-climb-game`（必须与代码中的 `GITHUB_REPO` 一致）
   - **描述**：输入 `3D爬山游戏V1`
   - **可见性**：选择 **公开**（Public）
   - **添加 README**：**勾选**（打钩）
   - **添加 .gitignore**：选择 **None**（或不勾选）
   - **添加许可证**：选择 **None**
5. 点击 **创建仓库**（Create Repository）

### 2.2 获取仓库地址

创建成功后，仓库会打开。点击页面右上角的 **三个点**（菜单按钮）：

- 选择 **复制URL**（Copy URL）
- 格式应为：`https://github.com/YOUR_USERNAME/mountain-climb-game.git`
- **记住这个地址**，后面会用到

### 2.3 在手机浏览器中上传文件到仓库

**GitHub APP 不支持上传文件夹**，必须使用网页版：

1. 打开**手机浏览器**（Chrome/Edge/Safari）
2. 访问 `https://github.com/YOUR_USERNAME/mountain-climb-game`
3. 确保已登录你的 GitHub 账号

#### 上传整个项目（文件较多，分批上传）

由于 GitHub 网页版一次只能上传最多 100 个文件，需要分批操作：

**方法A：通过电脑上传（如果有电脑时）**
```bash
cd mountain-climb-game
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/YOUR_USERNAME/mountain-climb-game.git
git push -u origin main
```

**方法B：纯手机网页版上传（推荐没有电脑时使用）**

1. 在浏览器中打开你的 GitHub 仓库页面
2. 点击 **Add file** 按钮
3. 选择 **Upload files**
4. 点击 **choose your files**
5. 进入文件管理器，导航到解压后的 `mountain-climb-game/` 文件夹
6. **长按**文件夹，选择 **全选**（Select All），然后 **上传**
   - 注意：如果文件超过100个，GitHub会提示无法上传
   - 解决方法：先上传核心文件，再分批上传其余文件

**核心文件优先上传顺序**（确保 GitHub Actions 能运行）：

第一批（根目录文件）：
```
build.gradle
settings.gradle
gradle.properties
gradlew
gradlew.bat
```

第二批（核心配置）：
```
core/build.gradle
core/src/ 下的所有 .java 文件
```

第三批（安卓模块）：
```
android/build.gradle
android/src/ 下的所有文件
android/assets/ 下的占位文件
```

第四批（GitHub Actions）：
```
.github/workflows/build-apk.yml
```

第五批（其他）：
```
gradle/wrapper/ 下的文件
README.md
GITHUB_SETUP.md
```

**注意**：上传时保持原有的文件夹结构，不要把所有文件平铺到根目录。

#### 简化方案：只上传必要文件

如果你嫌分批上传麻烦，可以**只上传这些关键文件**（约30个），GitHub Actions 就能编译：

```
mountain-climb-game/
├── build.gradle
├── settings.gradle
├── gradle.properties
├── gradlew
├── core/
│   ├── build.gradle
│   └── src/main/java/com/mountainclimb/game/
│       ├── GameConfig.java
│       ├── MountainClimbGame.java
│       ├── audio/
│       │   └── AudioManager.java
│       ├── input/
│       │   └── Joystick.java
│       ├── player/
│       │   └── Player.java
│       ├── save/
│       │   └── SaveManager.java
│       ├── screen/
│       │   ├── GameScreen.java
│       │   ├── MainMenuScreen.java
│       │   ├── MapSelectScreen.java
│       │   └── SettingsScreen.java
│       ├── update/
│       │   ├── UpdateListener.java
│       │   ├── UpdateManager.java
│       │   └── VersionInfo.java
│       └── world/
│           └── TerrainGenerator.java
├── android/
│   ├── build.gradle
│   ├── src/main/
│   │   ├── AndroidManifest.xml
│   │   └── java/com/mountainclimb/game/
│   │       ├── AndroidAudioLoader.java
│   │       └── AndroidLauncher.java
│   └── assets/  （空文件夹即可）
└── .github/workflows/
    └── build-apk.yml
```

### 2.4 确认上传完成

上传完成后，在浏览器中刷新 GitHub 仓库页面，确认：
- ✅ `build.gradle` 存在
- ✅ `core/` 文件夹存在
- ✅ `android/` 文件夹存在
- ✅ `.github/workflows/build-apk.yml` 存在

---

## 第三步：GitHub Actions 自动编译APK

### 3.1 触发自动编译

代码上传到仓库后，GitHub Actions 会自动开始编译。通常会在 **30秒~2分钟内**自动触发。

**如何确认编译已触发**：
1. 在浏览器中打开你的 GitHub 仓库
2. 点击顶部菜单栏的 **Actions**（在 Code、Issues、Pull requests 旁边）
3. 你应该看到一条工作流记录：
   - 名称：`Build Android APK`
   - 状态：黄色圆圈（⏳ 进行中）或绿色勾（✅ 完成）

### 3.2 等待编译完成

编译过程大约需要 **5-15分钟**。你可以：
- 刷新页面查看进度
- 点击正在运行的工作流名称，查看详细日志
- 看到绿色勾号表示编译成功

**常见状态**：
- 🟡 黄色圆圈：正在编译
- ✅ 绿色勾号：编译成功
- ❌ 红色叉号：编译失败（需要查看日志排查）

### 3.3 下载APK文件

编译成功后：

1. 在 Actions 页面点击**已完成的工作流**
2. 向下滚动到页面底部，找到 **Artifacts** 区域
3. 看到两个下载链接：
   - `debug-apk`：调试版本（推荐首次测试用）
   - `release-apk`：发布版本（需要签名才能安装）
4. 点击 **debug-apk** 旁边的下载按钮
5. 浏览器会下载一个 ZIP 文件（`debug-apk.zip`）

### 3.4 安装APK到手机

1. 用 **ZArchiver** 打开下载的 `debug-apk.zip`
2. 解压得到 `android-debug.apk`
3. 点击 APK 文件，系统提示 **是否安装**
4. 如果提示 **禁止安装未知来源应用**：
   - 进入 **设置** -> **安全** -> **未知来源** -> 允许
   - 或点击弹窗上的 **设置** 直接跳转
5. 完成安装

---

## 第四步：配置热更新（可选，首次可跳过）

### 4.1 修改 GameConfig.java

安装后游戏可以运行，但热更新功能需要配置你的 GitHub 信息：

1. 在 GitHub 仓库页面找到 `core/src/main/java/com/mountainclimb/game/GameConfig.java`
2. 点击文件名进入
3. 点击右上角的 **铅笔图标**（Edit）
4. 找到这两行：
```java
public static final String GITHUB_OWNER = "YOUR_GITHUB_USERNAME";
public static final String GITHUB_REPO = "mountain-climb-game";
```
5. 将 `"YOUR_GITHUB_USERNAME"` 替换为你的真实 GitHub 用户名
6. 滚动到页面底部，填写提交信息：
   - **Commit message**：输入 `Update GitHub config`
   - 选择 **Commit directly to the main branch**
7. 点击 **Commit changes**

### 4.2 创建 update 文件夹（用于发布热更新）

1. 在 GitHub 仓库首页点击 **Add file** -> **Create new file**
2. 文件名输入：`update/version.json`
3. 文件内容输入：
```json
{
    "versionCode": 2,
    "versionName": "1.0.1",
    "updateUrl": "https://github.com/YOUR_USERNAME/mountain-climb-game/releases/download/v1.0.1/patch.zip",
    "updateLog": "首次热更新测试",
    "forceUpdate": false
}
```
4. 点击 **Commit new file**

---

## 第五步：重新编译（修改代码后）

每次修改代码后，需要重新编译获取新版APK：

1. 在 GitHub 上编辑文件并提交
2. 提交后，GitHub Actions 会**自动重新编译**
3. 等待编译完成（查看 Actions 页面）
4. 下载新的 APK
5. 安装覆盖旧版本

---

## 常见问题

### Q: GitHub Actions 编译失败怎么办？
**A**: 点击失败的 Actions 记录 -> 查看日志 -> 找到错误信息。常见原因：
- 文件未上传完整：重新上传缺失文件
- 文件夹结构错误：检查 `core/` 和 `android/` 是否在正确位置

### Q: 上传文件时提示"超过100个文件"？
**A**: GitHub网页版限制。解决方案：
- 分批上传（每次不超过100个文件）
- 或只上传关键文件（见2.4节的简化方案）

### Q: 手机浏览器上传文件很慢？
**A**: 正常现象。建议：
- 使用WiFi上传
- 如果文件太多，先用ZArchiver打包成zip上传到GitHub，然后在线解压（GitHub不支持在线解压，所以还是建议分批）

### Q: 编译成功但APK无法安装？
**A**: 
- 确保开启了"允许安装未知来源应用"
- 如果提示"解析包错误"，可能是下载不完整，重新下载
- 部分手机需要在"开发者选项"中关闭"MIUI优化"（小米手机）

### Q: 没有网络或网络很慢？
**A**: GitHub Actions 在云端编译，不消耗你的手机流量。但如果 GitHub 访问困难，可以：
- 使用加速器/VPN
- 或改用 Termux 方案（见手机编译详细步骤.md的方案二）

---

## 完整流程图

```
解压项目 → 打开GitHub APP创建仓库 → 复制仓库地址
    ↓
打开浏览器访问仓库网页 → Add file → Upload files
    ↓
选择项目文件（分批上传） → Commit changes
    ↓
点击 Actions 标签 → 等待编译完成（5-15分钟）
    ↓
下载 debug-apk → 解压ZIP → 安装APK
    ↓
在手机上运行游戏！
```

---

## 下一步

1. **测试游戏**：运行APK，测试主菜单、设置、3D场景、摇杆操控
2. **添加资源**：准备真实的背景图和音效，替换占位文件
3. **发布更新**：修改代码后提交，GitHub Actions 自动编译新版
4. **配置热更新**：按 GITHUB_SETUP.md 的详细指南配置

**如果你在任何步骤卡住，请告诉我具体是哪一步，我会进一步详细解释！**
